dotnet new sln
dotnet new webapi -n API -controllers
dotnet new classlib -n Core
dotnet new classlib -n Infrastructure

#dotnet new webapi -o API -controllers
#dotnet new classlib -o Core
#dotnet new classlib -0 Infrastructure

docker compose up -d

 "DefaultConnection": "Server=localhost,1433;Database=skinet;User Id=SA;Password=Password@1;TrustServerCertificate=True"

 https://www.nuget.org/packages/dotnet-ef

Cd api
 dotnet tool install --global dotnet-ef --version 9.0.9

 dotnet ef migrations add InitialCreate -s API -p Infrastructure
 dotnet ef database update -s API -p Infrastructure

 ch03
 Introduction about AddScope AddTransient AddSingleton ch03-03 06:16

 dotnet ef database drop -p Infrastructure -s API

 ch06
 https://learn.microsoft.com/en-us/aspnet/core/fundamentals/middleware/?view=aspnetcore-9.0

 ch14
  dotnet ef migrations add IdentityAdded -s API -p Infrastructure
dotnet ef migrations add AddressAdded -s API -p Infrastructure
dotnet ef database drop -s API -p Infrastructure
dotnet ef database update -s API -p Infrastructure

ch16
dotnet ef migrations add DeliveryMethodsAdded -p Infrastructure -s API
dotnet tool update dotnet-ef -g

ch17
dotnet ef migrations add OrderAggregateAdded -p Infrastructure -s API

$env:DOTNET_SYSTEM_CONSOLE_ALLOW_ANSI_COLOR_REDIRECTION=1
setx DOTNET_SYSTEM_CONSOLE_ALLOW_ANSI_COLOR_REDIRECTION 1 